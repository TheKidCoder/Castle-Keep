require "spec_helper"

describe Castle::Keep do
  it "has a version number" do
    expect(Castle::Keep::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
